# LLM Council — environment configuration
#
# Copy this file to `.env` and fill in your OpenRouter API key.
# `.env` is gitignored — only this template is tracked.

# OpenRouter API key (required).
# Get yours at: https://openrouter.ai/keys
# Browse models and pricing: https://openrouter.ai/models
#
# A single OpenRouter key gives unified access to ~25 frontier models
# from Anthropic, OpenAI, Google, xAI, Meta, Mistral, DeepSeek and more.
# Typical cost per query in this app: USD $0.01–0.30 depending on council.
OPENROUTER_API_KEY=
